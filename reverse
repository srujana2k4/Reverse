#include<stdio.h>
#include<conio.h>
void main()
{
	int num,rem,rev=0;
	printf("Enter the number");
	scanf("%d",&num);
	while(num!=0)
	{
		rem=num%10;
		rev=rem+(rev*10);
		num=num/10;
	}
	printf("Reverse is %d",rev);
getch();	
}
